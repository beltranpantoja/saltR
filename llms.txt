# saltr [![package logo](reference/figures/saltr_logo.png)](https://beltranpantoja.github.io/saltR/)

The goal of saltR, pronounced as the spanish word *saltar*, is to
simplify the running of simulations in the context of DCMs by providing
utility functions that can make your code more readable and let you jump
straight into the simulation design without worrying too much about the
implementation.

## Why?

Normally, psychometric libraries have functions that generate
simulations, the downside is that it all happens inside a black box.
This library is meant to only provide semantic functions that do one
simple thing at a time.

Another difference with traditional psuchometric libraries is that saltr
tries to be more integrated with the tidyverse packages. For example,
`extract_item` takes the model and returns a tibble for easier work in a
pipeline.

## Get Started

You can install saltr from
[GitHub](https://github.com/beltranpantoja/saltR) with:

``` r
devtools::install_github("beltranpantoja/saltR")
```

And then jump to [Creating a single
simulation](https://beltranpantoja.github.io/saltR/vignettes/Creating-a-single-simulation.Rmd)
tutorial

# Package index

## Create

Utility functions for creating deterministic objects quickly.

- [`create_patterns()`](https://beltranpantoja.github.io/saltR/reference/create_patterns.md)
  : Create all possible permutations of the levels passed.
- [`create_qmatrix()`](https://beltranpantoja.github.io/saltR/reference/create_qmatrix.md)
  : Create Q-Matrix

## Generate

Functions that randomly generate base elements for a simulation.

- [`generate_examinees()`](https://beltranpantoja.github.io/saltR/reference/generate_examinees.md)
  : Generate sample of examinees
- [`generate_responses()`](https://beltranpantoja.github.io/saltR/reference/generate_responses.md)
  : Generate responses to a test

## Build

Functions that randomly generate base elements for a simulation.

- [`build_qmatrix()`](https://beltranpantoja.github.io/saltR/reference/build_qmatrix.md)
  : Build a Q-Matrix from a a matrix of test parameters
- [`build_response_likelihood()`](https://beltranpantoja.github.io/saltR/reference/build_response_likelihood.md)
  : Create the likelihood matrix for a matrix of probabilities of
  correct response for non-masters and masters marginal only on that
  attribute.
- [`build_test_parameters()`](https://beltranpantoja.github.io/saltR/reference/build_test_parameters.md)
  : Create a matrix of items and their parameters

## Get

Functions to extract information from a gdina object.

- [`get_probabilities_by_profile()`](https://beltranpantoja.github.io/saltR/reference/get_probabilities_by_profile.md)
  : Returns a matrix of profiles and the probability of a correct
  response for all items.
- [`get_test_parameters()`](https://beltranpantoja.github.io/saltR/reference/get_test_parameters.md)
  : Returns a matrix item-parameters from model

## Change

Change objects related to a model (e.g. a Q-matrix)

## Utils

Library utils

- [`check_monotonicity()`](https://beltranpantoja.github.io/saltR/reference/check_monotonicity.md)
  : Check that the monotonicity condition of the gdina model is
  fullfilled. This is useful for when you don't want to force it, but
  you want to know if the model was able to achieve it.
- [`check_test_parameters()`](https://beltranpantoja.github.io/saltR/reference/check_test_parameters.md)
  : Check if the test parameters are compatible with a Q-matrix
- [`fit_lcdm()`](https://beltranpantoja.github.io/saltR/reference/fit_lcdm.md)
  : Utility function to fits a LCDM model using the CDM package with
  some convenient defaults.
- [`pretty_print()`](https://beltranpantoja.github.io/saltR/reference/pretty_print.md)
  : Prints a matrix to the console in a prettier format

## Data

Data included in the package

- [`data.item_influence`](https://beltranpantoja.github.io/saltR/reference/data.item_influence.md)
  : Item Influence for DCM.

# Articles

### Examples

- [Running a
  simulation](https://beltranpantoja.github.io/saltR/articles/run_simulation.md):
